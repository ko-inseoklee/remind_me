server:
  port: 8080

spring:
  application:
    name: remind-me-server

  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?user=postgres.gblnlgamxmrwfervtgjc&password=isleedb2602
    driver-class-name: org.postgresql.Driver

  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        # dialect: org.hibernate.dialect.PostgreSQLDialect
        transaction:
          jta:
            platform: org.hibernate.service.jta.JtaPlatform

  # Redis 설정
  data:
    redis:
      host: localhost
      port: 6379

# Swagger (SpringDoc) 설정
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    groups-order: asc
  api-docs:
    path: /api-docs